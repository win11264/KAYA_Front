import React from "react";

const ExcList = ({ children }) => {
  return (
    <div className='bg-white py-3 w-100 d-flex flex-column justify-content-center align-items-center'>
      {children}
    </div>
  );
};

export default ExcList;
